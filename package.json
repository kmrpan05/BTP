{
  "name": "bookshop",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@sap/cds": "^10.1.0"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^3.1.0",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "@sap/eslint-plugin-cds": "^4"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch"
  },
  "cds" :{
    "requires": {
      "db": {
       "[testing]": { "kind": "sqlite", "credentials": {"url": "bookshop.sqlite" }},
       "[development]": { "kind": "sqlite", "credentials": {"url":":memory:"}}        
      }
    }
  }
}
