'use strict'

const { FILES } = require('../constants')

/**
 * Returns an array of allowed file extensions
 * the plugin can parse of the form "*.ext"
 * @returns { import('../types').ConfigOverrideFiles } Array of file extensions
 */
module.exports = () => {
  return FILES
}
